<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylish.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Her Website</title>
</head>
<body>
    <p>Happy Valentine's Day ❤️</p>

    <div class="center">
        <div class="lock">
            <h3>🔒Enter your password here</h3> 
            <!-- <img src="hello.jpg" alt="" class="lock-image"> -->
            <input type="hidden" name="pin" id="pin">
            <div class="pin_display">
                <div class="dots">
                    <!-- <div class="dot"></div> -->
                </div>
            </div>
            <div class="keys">
                <button type="button">1</button>
                <button type="button">2</button>
                <button type="button">3</button>
                <button type="button">4</button>
                <button type="button">5</button>
                <button type="button">6</button>
                <button type="button">7</button>
                <button type="button">8</button>
                <button type="button">9</button>
                <button type="button">
                    <span class="material-icons-outlined">*</span>
                </button>
                <button type="button">0</button> 
                <button type="button">

                    <span> <a href="./file2.html"> GO  </span> </a>

                </button>
            </div>
        </div>
    </div>
    <script>
    document.addEventListener("DOMContentLoaded", () => {
        const input = document.getElementById("pin");
        const dotsContainer = document.querySelector(".dots"); // Fixed the misplaced comment
        const keys = document.querySelectorAll(".keys button");
    
        keys.forEach((key) => {
            key.addEventListener("click", (e) => {
                // if (e.target.closet("clear")){
                //     removeDot();
                //     input.value = input.value.slice(0,-1);
                // }
                input.value += key.innerText;
                addDot();
            });
        });
    
        function addDot() {
            const dot = document.createElement("div");
            dot.className = "dot";
            dotsContainer.appendChild(dot);
        }
    
        function removeDot() {
            const dots = dotsContainer.querySelectorAll(".dot");
            if (dots.length > 0) {
                const lastDot = dots[dots.length - 1];
                lastDot.classList.add("removing");
                setTimeout(() => {
                    dotsContainer.removeChild(lastDot);
                }, 100);
            }
        }
    });
    
</script>
    
</body>
</html>